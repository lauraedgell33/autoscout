# ================================================
# LARAVEL CLOUD ENVIRONMENT VARIABLES
# ================================================
# Copy these to: https://cloud.laravel.com/anemette-madsen
# Project: autoscout-safetrade
# Environment: Production → Environment Variables
# ================================================

# Application
APP_NAME="AutoScout24 SafeTrade"
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:AWs0dTYjNtTmoDlPy+mt8joPQfJcabdGuJeCwAMsB4o=
APP_TIMEZONE=UTC
APP_URL=https://REPLACE_WITH_VAPOR_URL.vapor-farm-x1.com

# Locale
APP_LOCALE=en
APP_FALLBACK_LOCALE=en

# Database (Auto-configured by Vapor - these are examples)
DB_CONNECTION=mysql
# DB_HOST, DB_PORT, DB_DATABASE, DB_USERNAME, DB_PASSWORD
# Will be automatically set by Vapor when you create database

# Cache & Session
CACHE_DRIVER=redis
CACHE_PREFIX=autoscout
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_DOMAIN=

# Queue
QUEUE_CONNECTION=sqs
# SQS settings auto-configured by Vapor

# Redis (Auto-configured by Vapor)
# REDIS_HOST, REDIS_PASSWORD, REDIS_PORT
# Will be automatically set by Vapor when you create cache

# Logging
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# AWS (Auto-configured by Vapor)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# These are set automatically by Vapor

# Mail
MAIL_MAILER=log
# For production, use SES:
# MAIL_MAILER=ses
# MAIL_FROM_ADDRESS=noreply@autoscout24.com
# MAIL_FROM_NAME="${APP_NAME}"

# Frontend CORS (Update after Vercel deployment)
FRONTEND_URL=https://REPLACE_WITH_VERCEL_URL.vercel.app
SANCTUM_STATEFUL_DOMAINS=REPLACE_WITH_VERCEL_URL.vercel.app
SESSION_DOMAIN=

# API
API_PREFIX=api

# Security
BCRYPT_ROUNDS=12

# Filesystem
FILESYSTEM_DISK=s3
# S3 bucket auto-configured by Vapor

# Broadcasting (if needed)
BROADCAST_DRIVER=log

# Sanctum
SANCTUM_MIDDLEWARE_APPEND_OR_REPLACE=replace

# Filament (Admin Panel)
FILAMENT_PATH=admin

# ================================================
# IMPORTANT NOTES:
# ================================================
# 1. Replace APP_URL after first deployment
# 2. Replace FRONTEND_URL after Vercel deployment  
# 3. Database, Redis, SQS credentials are auto-set by Vapor
# 4. For production email, configure AWS SES
# ================================================
