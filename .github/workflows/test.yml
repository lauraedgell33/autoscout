name: Run Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  backend-tests:
    name: Backend Tests (Laravel)
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, pdo, pdo_mysql, bcmath, gd, zip
          coverage: none

      - name: Install Dependencies
        run: |
          cd scout-safe-pay-backend
          composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Setup Test Database
        run: |
          cd scout-safe-pay-backend
          cp .env.example .env
          php artisan key:generate
          touch database/database.sqlite

      - name: Run Migrations
        run: |
          cd scout-safe-pay-backend
          php artisan migrate --force
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite

      - name: Check Application Health
        run: |
          cd scout-safe-pay-backend
          php artisan about
          echo "✅ Laravel application is healthy"

      - name: Notify on Success
        if: success()
        run: echo "✅ Backend checks passed successfully!"

  frontend-build:
    name: Frontend Build (Next.js)
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: scout-safe-pay-frontend/package-lock.json

      - name: Install Dependencies
        run: |
          cd scout-safe-pay-frontend
          npm install

      - name: Build Frontend
        run: |
          cd scout-safe-pay-frontend
          npm run build
        env:
          NEXT_PUBLIC_API_URL: https://adminautoscout.dev/api

      - name: Notify on Success
        if: success()
        run: echo "✅ All tests and builds passed!"
