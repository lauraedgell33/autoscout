{
  "private": true,
  "scripts": {
    "build": "cd scout-safe-pay-frontend && npm install && npm run build",
    "dev": "cd scout-safe-pay-frontend && npm run dev",
    "start": "cd scout-safe-pay-frontend && npm run start"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.1",
    "@testing-library/user-event": "^14.6.1",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0"
  }
}
